<h1 align="center"><%= "Category: " + @category.name %></h1><hr>
<div class="center new-article col-xs-10 col-xs-offset-1">
 <%= link_to "Create New Article", new_article_path, class: "btn btn-sm btn-primary" %>
</div>
 
<div align="center">
    <% if logged_in? and current_user.admin? %>
      <div class="row">
        <span class="badge"><%= link_to "Edit Category Name", edit_category_path(@category) %></span>
      </div>
    <% end %>  
    <%= will_paginate @category_articles %>
</div> 
<%= render 'articles/article', obj: @category_articles %>
<div align="center">
    <%= will_paginate @category_articles %>
</div>